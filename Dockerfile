# Root Dockerfile (documentation only)
# This project uses individual Dockerfiles for each service:
# - backend/Dockerfile: FastAPI backend
# - web-ui/Dockerfile: Next.js frontend
#
# To build and run services, use the Dockerfiles in their respective folders.
# This file is a placeholder and not intended to build an image.
